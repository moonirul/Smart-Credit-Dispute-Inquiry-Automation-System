[project]
name = "smart-credit-dispute-inquiry-automation-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "monirul", email = "monirrul.cse@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "openai>=3.3.1",
    "paddleocr>=3.7.0",
    "paddlepaddle>=3.3.1",
    "pymupdf>=1.28.2",
    "python-dotenv>=1.2.3",
    "python-multipart>=0.0.32",
    "uvicorn>=0.52.4",
]

[project.scripts]
smart-credit-dispute-inquiry-automation-system = "smart_credit_dispute_inquiry_automation_system:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
